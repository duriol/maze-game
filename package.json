{
  "name": "maze-game",
  "version": "1.0.0",
  "description": "Isometric maze game with fog of war",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "npm run build && cd dist && zip -r ../maze-game.zip . && cd .. && echo '\n✅ Juego empaquetado en maze-game.zip'",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
